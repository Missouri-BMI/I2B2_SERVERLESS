<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<name>SHRINE Utility Code</name>
	<artifactId>shrine-util</artifactId>
	<packaging>jar</packaging>
	<parent>
		<groupId>net.shrine</groupId>
		<artifactId>shrine-base</artifactId>
		<version>4.4.0</version>
		<relativePath>../../pom.xml</relativePath>
	</parent>

	<dependencies>
		<dependency>
			<groupId>io.circe</groupId>
			<artifactId>circe-generic_${scala-major-version}</artifactId>
			<version>${circe-version}</version>
		</dependency>
		<dependency>
			<groupId>org.scala-lang.modules</groupId>
			<artifactId>scala-xml_${scala-major-version}</artifactId>
			<version>${scala-xml-version}</version>
		</dependency>
		<dependency>
			<groupId>com.typesafe.slick</groupId>
			<artifactId>slick_${scala-major-version}</artifactId>
			<version>${slick-version}</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-core</artifactId>
			<version>${logback-version}</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>${logback-version}</version>
		</dependency>
		<dependency>
			<groupId>org.http4s</groupId>
			<artifactId>http4s-core_${scala-major-version}</artifactId>
			<version>${http4s-version}</version>
		</dependency>
		<dependency>
			<groupId>org.http4s</groupId>
			<artifactId>http4s-servlet_${scala-major-version}</artifactId>
			<version>${http4s-servlet-version}</version>
		</dependency>
		<dependency>
			<groupId>org.http4s</groupId>
			<artifactId>http4s-jdk-http-client_${scala-major-version}</artifactId>
			<version>${http4s-jdk-http-client-version}</version>
		</dependency>
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>javax.servlet-api</artifactId>
			<version>${servlet-api-version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>com.github.daddykotex</groupId>
			<artifactId>courier_${scala-major-version}</artifactId>
			<version>${courier-version}</version>
		</dependency>
		<dependency>
			<groupId>org.bouncycastle</groupId>
			<artifactId>bcpkix-jdk15on</artifactId>
			<version>${bouncy-castle-version}</version>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<version>${h2-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.jvnet.mock-javamail</groupId>
			<artifactId>mock-javamail</artifactId>
			<version>${mock-javamail-version}</version>
			<scope>test</scope>
		</dependency>
		<!--todo either rip this out or capture the version number -->
		<dependency>
			<groupId>org.xerial</groupId>
			<artifactId>sqlite-jdbc</artifactId>
			<version>3.15.1</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.typelevel</groupId>
			<artifactId>cats-effect-testkit_${scala-major-version}</artifactId>
			<version>${cats-effect-testkit-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.scalatest</groupId>
			<artifactId>scalatest_${scala-major-version}</artifactId>
			<version>${scalatest-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.http4s</groupId>
			<artifactId>http4s-dsl_${scala-major-version}</artifactId>
			<version>${http4s-version}</version>
			<scope>test</scope>
		</dependency>
		<!-- Replace older transitive dependency pulled in by Squeryl -->
		<dependency>
			<groupId>org.scala-lang</groupId>
			<artifactId>scalap</artifactId>
			<version>${scala-version}</version>
		</dependency>
	</dependencies>

	<build>
		<sourceDirectory>src/main/scala</sourceDirectory>
		<testSourceDirectory>src/test/scala</testSourceDirectory>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>net.alchim31.maven</groupId>
				<artifactId>scala-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>${maven-jar-plugin-version}</version>
				<executions>
					<execution>
						<goals>
							<goal>test-jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
