referenceTestTarget = "from reference.conf"

overrideReferenceTestTarget = "from reference.conf"

//todo SHRINE2020-1438 this can't be done because slick's GlobalConfig calls ConfigFactory.load(). Need a way to pass it our config instead
//requiredByReferenceConfTestTarget = ${requiredByReferenceConfValue}

//todo SHRINE2020-1438 this can't be done because slick's GlobalConfig calls ConfigFactory.load(). Need a way to pass it our config instead
//overrideVariablesTestTarget = ${overrideVariablesConfValue}
