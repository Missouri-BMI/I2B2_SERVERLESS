# Send .php to php-fpm over Unix socket
<FilesMatch \.php$>
    SetHandler "proxy:unix:/run/php-fpm/www.sock|fcgi://localhost/"
</FilesMatch>

DirectoryIndex index.php index.html
# Optional timeouts for heavier PHP requests
ProxyTimeout 300